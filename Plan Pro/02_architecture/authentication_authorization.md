# Authentication and Authorization (MVP)

- Authentication (AuthN) is delegated to an external managed service: **Firebase Auth**.
- Authorization (AuthZ) is enforced by the system’s business logic (application layer + data access scoping).

More details: TBD.

